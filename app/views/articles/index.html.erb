<div class="h-24 bg-gradient-to-r from-teal-400 to-blue-500"></div>
<div class="text-center p-4 hover:bg-gray-400">
  <h1 class="text-2xl font-medium text-gray-800">Leanios MES</h1>
  <blockquote class="text-lg font-medium text-gray-800 italic mt-4">
   "Knowledge is not just a product of individual minds, but also a product of social networks and the distributed intelligence of communities and organizations." 
  </blockquote>
    <%= current_user&.email %>
    <br>
    <%= link_to 'logout', destroy_user_session_path, method: :delete, class: "bg-gray-700 text-white py-2 px-4 decoration-auto inline-block text-base cursor-pointer rounded-3xl mx-auto my-4"  %>
</div>
<div class="text-center">

  <%= link_to "New Article", new_article_path, class: "bg-gray-500 text-white py-2 px-4 decoration-auto inline-block text-base cursor-pointer rounded-3xl mx-auto my-4" %>
  <div class="bg-gray-100 p-6 rounded-lg">
    <h2 class="text-lg font-semibold mb-4">Steps for Leanios Account Configuration</h2>
  </div> 
</div>
<br>

<div class="grid grid-col-1 gap-4 sm:grid-cols-2 md:grid-cols-4 ">
    <% @articles.each do |article| %>
      <% unless article.archived? %>
          <div class="w-full rounded-lg overflow-hidden shadow-lg">
            <div class="font-bold text-xl mb-2"> 
                <% if article.picture.present? %>
                  <p class="rounded-md mt-3 m-auto w-fit"><%= image_tag article.picture, style: "width: 400px; height: 500px;"%></p>
                <% end %>
                <p class="mt-3 m-auto w-fit"><%= link_to article.title, article, class: "px-4 py-2 text-lg font-bold text-blue-500 bg-gray-300 rounded hover:bg-gray-400" %></p>
            </div>
             <% article.comments.each do |comment| %>
                      <p><div class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2">
                        <%= comment.commenter %>:
                        <%= comment.body %>
                      </div></p>
              <% end %>  
              <p class="text-sm font-bold text-gray-600">
                Author: <%= article.user.email if article.user %>
              </p>
          </div>
      <% end %>
    <% end %>
</div>
